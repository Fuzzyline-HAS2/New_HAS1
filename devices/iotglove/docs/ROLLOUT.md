# 조립 전 검증과 7대 설치

2026-09-17 기준. G1P1 한 쌍에서 GPIO1 수정본 USB 설치·UART·원격 리셋에 이어 **Beetle→TTGO v1→v2 OTA와 Telnet 상태·PING·Beetle BOOT 로그·재접속을 확인**했다. 두 보드 모두 펌웨어 2 / 파티션 1이다. 현재 사용자 요청의 다음 단계는 기존 G1P1을 포함한 **총 7대의 초기 USB 설치·MAC 등록**이며, G1P2는 업로드 진행 중이며 G1P3~G1P7은 대기 중이다. 기존 펌웨어 백업 없이 현재 Release를 설치하고, 업로드 중 MAC 등록은 사용자가 직접 진행한다. OTA·Telnet 실측은 7대 전체 업로드 후 일괄 수행한다. 세부 근거는 [검증 기록](VALIDATION.md)을 따른다.

## 1. 현재 한 쌍으로 OTA·Telnet 실증 — 완료

- [x] 등록된 G1P1의 유효한 서버 응답과 비활성 `ready` 상태, 현재 버전·파티션·부팅 증거를 확인했다. 두 대상 Actions 성공과 Release 산출물의 HMAC을 확인했다.
- [x] `github@2:2`로 **Beetle v2 업데이트 → 새 부팅·목표 버전 확인 → TTGO v2 업데이트·새 부팅**을 실증했다. flashing 또는 동일 버전 skip만으로 실제 설치를 판정하지 않았다.
- [x] TTGO 재접속 후 양쪽 펌웨어 2 / 파티션 1, UART 응답, 서버 `valid=1`·`fresh=1`·`esp_version=2`를 확인했다.
- [x] Telnet 첫 접속과 재접속에서 상태·수동 PING 각 2회를 확인했다. 마지막 부팅 후 PING 누계는 45/45회, timeout 0회다. 명시적 Beetle BOOT 원인 값 3, 반복 PING 중 BOOT 이벤트 중복 억제와 로그 출력 중 UART 응답을 확인했다.

최초 업데이트 당시 TTGO v1은 Beetle LOG를 소비하지 않는다. OTA 순서·재부팅 증거는 UART 기록, BOOT 로그·상태·PING은 v2 설치 후 Telnet 기록으로 구분한다. 이번 완료는 전체 OTA 과정을 Telnet에서 관찰하거나 롤백·장애 복구를 실측했다는 의미가 아니다.

## 2. G1P1~G1P7, 총 7대 초기 USB 설치·MAC 등록 — 1대 완료, 1대 진행, 5대 대기

각 장치는 TTGO와 Beetle 한 쌍이다. G1P1은 v1 최초 USB 설치 후 위 OTA로 v2를 확인했으므로 USB 재설치는 필요 없다. 나머지 장치는 **기존 펌웨어를 백업하지 않고 현재 확정한 v2 Release 이미지로 바로 설치**한다. 업로드 시 실물 라벨·보드 종류·MAC을 대조해 장치를 식별하며, MAC 등록은 사용자가 업로드와 병행한다. MAC/IP·업로드 원본 로그는 로컬 장치 목록에 보관하고 공개 문서와 PR에는 해당 주소 목록이나 비밀값을 넣지 않는다.

| 장치 | 현재 상태 |
| --- | --- |
| G1P1 | 두 보드 초기 USB 설치·서버 등록, v2 순차 OTA·Telnet·UART 확인 완료 |
| G1P2 | 두 보드 업로드 진행 중, MAC 등록 완료 |
| G1P3~G1P7 | 각 두 보드 업로드 대기, 업로드 중 사용자가 MAC 등록 |

G1P2의 MAC은 이미 등록했다. 이후 장치의 서버 MAC 등록은 사용자가 직접 담당한다.

업로드 단계에서는 아래 항목을 장치별로 수행하고 다음 장치로 넘어간다.

- [ ] 실물 라벨·보드 종류·MAC으로 업로드 대상을 식별하고 TTGO MAC을 사용자에게 전달한다. 사용자는 올바른 서버 장치명에 직접 등록한다.
- [ ] 각 보드의 GitHub Actions Release에서 내려받아 서명을 검증한 `update.bin`과 **동일한 애플리케이션 바이트**, `min_spiffs` 파티션으로 바로 설치한다. 기존 펌웨어 백업은 수행하지 않으며 compile-only 이미지는 사용하지 않는다.
- [ ] 업로드의 부트로더·파티션·boot_app0·애플리케이션 **네 영역 해시 검증**과 설치 대상 펌웨어 2 / 파티션 1을 기록한다. 장치별 업로드 결과와 사용자의 MAC 등록 완료 여부를 로컬 목록에 구분해 남긴다.

**장치마다 OTA·Telnet 실측을 끼워 넣지 않는다.** G1P1을 포함한 7대 전체 업로드가 끝난 뒤 아래 확인을 일괄 진행한다.

- [ ] 사용자의 MAC 등록 결과와 7대의 Wi-Fi·유효한 서버 응답, 실행 펌웨어·파티션을 대조한다.
- [ ] 7대의 UART 일치 PING, 리셋 후 새 boot ID·통신 복구, Telnet 접속·재접속과 OTA 결과를 일괄 확인한다. 동일 버전 skip은 실제 새 이미지 설치와 구분하고, 한 대의 결과를 다른 장치의 완료 근거로 사용하지 않는다.
- [ ] 일괄 확인 결과를 장치별 로컬 목록에 갱신한다. 실패 장치는 재현 조건과 기대 동작을 적어 별도 서브아이템으로 추적하고 상태를 구분한다.

게임 전체·BLE 매핑·배터리 보정은 아래 후속 검증에서 관리하며, 이번 초기 설치·등록의 추가 선행 조건으로 두지 않는다.

## 3. 이후 개발: PR·Actions·운영 OTA

첫 v2의 두 대상 Actions·Release 게시·서명 확인과 G1P1 적용은 완료됐다. 이후 변경은 다음 절차로 진행한다.

- [ ] PR에서 소스·문서·서명/보관 정책을 독립 검토하고 host/Python 테스트 및 TTGO 본게임·훈련·Beetle 컴파일 결과를 확인한다. [서버 계약](SERVER_CONTRACT.md)의 미해결 항목과 실측 범위를 명시한다.
- [ ] 변경 반영 후 기존 `Deploy Firmware` 규칙으로 두 대상을 하나씩 배포한다. 정수 버전은 CI가 증가시키며 고정/보관 릴리즈의 자산·서명·출처를 확인한다. 여러 실행을 동시에 대기열에 넣지 않는다.
- [ ] 새로운 운영 OTA는 먼저 한 쌍의 목표 버전·새 부팅·재연결을 확인한 뒤 설치된 나머지 6대로 확대한다. 두 보드 업데이트는 원자적이지 않으므로 부분 성공 시 각 실행 버전을 확인하고 복구한다.
- [ ] 설치 버전과 검증 일자를 로컬 목록에 갱신하고 공개 기록에는 주소·비밀값을 제거한 결과를 남긴다. 게시·설치·실측 완료를 구분한다.

## 4. 별도 후속 검증 — 미완료

- 실제 역방향 롤백·한 보드만 변경·동일 버전 skip, 없는 버전·서명 오류·파티션 불일치·통신 단절·NVS/플래시 기록 중 전원 손실의 처리.
- 전체 게임/훈련 사이클, 칩·버튼·LED·진동과 서버 계약의 미해결 구간.
- 실제 비콘 매핑, 방 이동·경계·비콘 소실과 위치/진동 처리.
- 배터리 ADC 배선·분압비·멀티미터 보정 및 부하별 확인. 현재 측정 기본 비활성 상태를 측정 완료로 기록하지 않는다.
- WDT 원인 판독을 포함한 리셋 장애 시나리오. v2 BOOT 원인 값 수신만으로 과거 리셋 시험의 원인을 확정하지 않는다.

후속 검증에서 확인되는 문제·기획 차이는 재현 조건과 함께 별도 서브아이템으로 나누어 개발한다.
