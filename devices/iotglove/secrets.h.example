#pragma once

// Copy locally to secrets.h and provide the same signing key as release CI.
// Never commit secrets.h. The build-check script supplies an isolated placeholder.
#define HMAC_SECRET "REPLACE_WITH_DEPLOYMENT_SECRET"
